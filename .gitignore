# Jekyll build / cache
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/

# macOS
.DS_Store

# Internal proposal — keep local, don't publish to the public repo.
# Delete this line if you want it in the repo.
corl_workshop.pdf
